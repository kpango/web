/** Reusable Tailwind class strings shared across components and routes. */

/** Centered content container with responsive horizontal padding. */
export const containerClass = "max-w-4xl mx-auto px-4 sm:px-6 lg:px-8";

/** Primary heading / title text color. */
export const textPrimary = "text-foreground";

/** Secondary / muted text color. */
export const textSecondary = "text-muted-foreground";

/** Accent text color (indigo). */
export const textAccent = "text-primary";

/** Card surface: background, text, rounded corners, border. Use `rounded-xl` for smaller cards. */
export const cardClass = "bg-card text-card-foreground rounded-2xl border border-border";

/** Date / mono text style used in timelines and blog metadata. */
export const dateMonoClass = "text-xs text-muted-foreground whitespace-nowrap font-mono";
